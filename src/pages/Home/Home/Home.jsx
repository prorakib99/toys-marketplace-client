import React from 'react';
import Hero from '../Hero/Hero';
import SearchBar from '../SearchBar/SearchBar';

const Home = () => {
    return (
        <>
            <Hero />
            <SearchBar />
        </>
    );
};

export default Home;
